# Ctrl-A + s => sync panes
# Ctrl-A + S => unsync panes
bind s setw synchronize-panes on
bind S setw synchronize-panes off

# Shift + -> => Move to next tab
# Shift + <- => Move to prev tab
bind-key -n S-Right next-window
bind-key -n S-Left previous-window
